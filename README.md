# J-Booking — Hotel Booking System

> **Финальный проект** стажировки.

J-Booking — сервис бронирования отелей. Всё начинается с выбора города: пользователь просматривает доступные города, переходит к списку отелей и запускает поиск с нужными фильтрами — даты заезда и выезда, количество гостей, ценовой диапазон, минимальная звёздность и набор удобств. Система возвращает только те отели, в которых есть свободные номера на указанный период. Цены отображаются в выбранной валюте — UZS, USD или RUB.

Когда номер выбран, запускается процесс бронирования. Он состоит из трёх шагов: сначала номер переходит в статус **HOLD** — он зарезервирован, но ещё не подтверждён. Если в течение 3 минут пользователь не подтверждает бронирование, оно автоматически отменяется. После подтверждения статус меняется на **CONFIRMED** — у пользователя есть 1 минута, чтобы перейти к оплате. Если оплата не поступает — бронирование сбрасывается обратно в HOLD. При успешной оплате статус становится **PAID**: сервис обращается к внешнему **Transaction Processing Service** для проведения транзакции, а затем через **Notification Service** отправляет уведомление гостю.

Если планы изменились — бронирование можно отменить в любой момент. Сервис автоматически рассчитает сумму возврата: при отмене более чем за 72 часа до заезда возвращается 100%, от 24 до 72 часов — 50%, менее чем за 24 часа — возврат не предусмотрен.

Помимо бронирования, гости могут оставлять отзывы об отелях и просматривать чужие. Для администраторов предусмотрен отдельный эндпоинт для обновления цен на номера.

---

## Жизненный цикл бронирования

```
POST /bookings  →  HOLD  →  POST /bookings/confirm  →  CONFIRMED  →  POST /bookings/pay  →  PAID
                    │                                      │                    ↓
                    │                ┌──────────────────────┘       Transaction Processing Service
                    │                ↓                               Notification Service
                    │        таймаут 1 мин
                    │        (Scheduler: CONFIRMED → HOLD)
                    │
                    └── таймаут 3 мин
                        (Scheduler: HOLD → CANCELLED)

PAID  →  PUT /bookings/{id}/cancel  →  CANCELLED  (возврат по политике отмены)
```

| Статус      | Описание                                                                        |
|-------------|---------------------------------------------------------------------------------|
| `HOLD`      | Номер удержан, ожидает подтверждения. Таймаут: 3 мин → автоотмена              |
| `CONFIRMED` | Бронирование подтверждено, ожидает оплаты. Таймаут: 1 мин → сброс в `HOLD`    |
| `PAID`      | Оплата прошла, уведомление отправлено. Можно отменить — возврат по политике     |
| `CANCELLED` | Бронирование отменено                                                           |

---

## Описание

J-Booking — REST API приложение для бронирования номеров в отелях. Реализует полный цикл бронирования: от инициализации до оплаты, с поддержкой отмены и политики возврата средств.

| Функциональность                  | Метод    | Эндпоинт                                  | Описание                                                                                  |
|-----------------------------------|----------|-------------------------------------------|-------------------------------------------------------------------------------------------|
| Просмотр городов                  | `GET`    | `/api/v1/cities`                          | Список всех городов (пагинация)                                                           |
| Список отелей по городу           | `GET`    | `/api/v1/hotels`                          | Список отелей по `cityId`                                                                 |
| Поиск отелей                      | `GET`    | `/api/v1/hotels/search`                   | Поиск с фильтрами: даты, кол-во гостей, город, цена, звёзды, удобства                    |
| Просмотр номеров отеля            | `GET`    | `/api/v1/rooms`                           | Список номеров по `hotelId` и `cityId`, с поддержкой валюты                               |
| Инициализация бронирования        | `POST`   | `/api/v1/bookings`                        | Создать бронирование, номер переходит в статус `HOLD`                                     |
| Подтверждение бронирования        | `POST`   | `/api/v1/bookings/confirm`                | Подтвердить бронирование, статус → `CONFIRMED`                                            |
| Оплата бронирования               | `POST`   | `/api/v1/bookings/pay`                    | Оплатить бронирование через Transaction Processing Service, статус → `PAID` + уведомление |
| Получить бронирование             | `GET`    | `/api/v1/bookings/{id}`                   | Получить бронирование по ID с поддержкой валюты                                           |
| Мои бронирования                  | `GET`    | `/api/v1/bookings/my`                     | Список бронирований гостя по PINFL (пагинация)                                            |
| Отмена бронирования               | `PUT`    | `/api/v1/bookings/{id}/cancel`            | Отменить бронирование с автоматическим расчётом возврата                                  |
| Информация о возврате             | `GET`    | `/api/v1/bookings/{id}/refund-info`       | Узнать сумму возврата до отмены                                                           |
| Создать отзыв                     | `POST`   | `/api/v1/hotels/{hotelId}/reviews`        | Оставить отзыв об отеле                                                                   |
| Список отзывов                    | `GET`    | `/api/v1/hotels/{hotelId}/reviews`        | Получить отзывы об отеле (пагинация)                                                      |
| Удалить отзыв                     | `DELETE` | `/api/v1/hotels/{hotelId}/reviews/{id}`   | Удалить свой отзыв                                                                        |
| Обновление цены номера (admin)    | `PATCH`  | `/api/v1/update-room-price`               | Обновить цену номера                                                                      |

Приложение взаимодействует с двумя внешними сервисами:
- **Transaction Processing Service** — обработка платёжных транзакций
- **Notification Service** — отправка уведомлений гостям

---

## Стек технологий

| Компонент        | Технология                              |
|------------------|-----------------------------------------|
| Язык             | Java 21                                 |
| Фреймворк        | Spring Boot 4.0.2                       |
| База данных      | PostgreSQL                              |
| Миграции БД      | Flyway                                  |
| HTTP-клиент      | Spring Cloud OpenFeign                  |
| Маппинг          | MapStruct                               |
| Документация     | Swagger / OpenAPI (springdoc)           |
| Шедулер          | Spring Scheduler + ShedLock             |
| Тесты            | JUnit 5, H2 (in-memory)                 |

---

## Запуск

### Требования
- Java 21+
- PostgreSQL (база `j_booking`, порт `5432`)
- Доступ к внешним сервисам (или их моки)

### Запуск приложения

```bash
./gradlew bootRun
```

### Swagger UI
После запуска документация доступна по адресу:
```
http://localhost:8080/swagger-ui/index.html
```

---

## Внешние сервисы (Feign клиенты)

### Transaction Processing Service
```
URL: http://localhost:7070
POST /api/v1/transactions
```
Используется при оплате подтверждённого бронирования (`/api/v1/bookings/pay`). Сервис выполняет списание средств и возвращает результат транзакции.

### Notification Service
```
URL: http://217.29.121.129/notification_api
POST /api/notifications/sending
```
Используется после успешной оплаты и при отмене бронирования. Отправляет уведомление главному гостю.

Настройка URL выполняется в `application.yml`:
```yaml
spring.cloud.openfeign.client.config:
  paymentClient.url: http://localhost:7070
  notificationClient.url: http://217.29.121.129/notification_api
```

---

## Политика отмены и возврата

| Период до заезда  | Возврат |
|-------------------|---------|
| Более 72 часов    | 100%    |
| От 24 до 72 часов | 50%     |
| Менее 24 часов    | 0%      |

---

## Поддержка валют

Цены возвращаются в запрошенной валюте. По умолчанию — `UZS`.

| Валюта | Курс (за 1 единицу в сумах) |
|--------|-----------------------------|
| UZS    | базовая                     |
| USD    | 12 000 сум                  |
| RUB    | 150 сум                     |

Параметр `currency` поддерживается в эндпоинтах: `GET /bookings/{id}`, `GET /bookings/my`, `GET /rooms`.

---

## Фоновые задачи (Scheduler)

- **BookingHoldScheduler** — каждые 60 секунд проверяет бронирования в статусе `HOLD`. Если таймаут (3 мин) истёк — переводит в `CANCELLED`.
- **BookingConfirmedScheduler** — каждые 30 секунд проверяет бронирования в статусе `CONFIRMED`. Если таймаут оплаты (1 мин) истёк — возвращает обратно в `HOLD`.

---

## Конфигурация (`application.yml`)

```yaml
booking:
  min-nights: 1
  hold-timeout-minutes: 3
  hold-check-interval-ms: 60000
  confirmed-timeout-minutes: 1
  confirmed-check-interval-ms: 30000
  cancellation:
    full-refund-hours: 72
    partial-refund-hours: 24

currency:
  usd-rate: 12000
  rub-rate: 150
```

---

## UI (Vaadin)

Помимо REST API, в проекте есть встроенный веб-интерфейс на Vaadin — полноценный дашборд, который позволяет вживую потрогать всю логику сервиса прямо в браузере, без Postman и curl.

Открыть его просто:
```
http://localhost:8080/
```

---

### Как устроен интерфейс

При открытии страницы вас встречает **Hero-баннер** — широкая карточка с фотографией отеля, заголовком «Hotel Explorer» и подписью. Это не просто картинка: сразу под баннером — полоска из трёх атмосферных фотографий направлений, задающая настроение «я уже выбираю путешествие».

Ниже — **панель фильтров**, разбитая на два ряда:

- **Первый ряд:** выбор города (выпадающий список), даты заезда и выезда (шаг 30 минут), количество гостей.
- **Второй ряд:** минимальный рейтинг звёзд (по умолчанию 3), диапазон цен, мультивыбор удобств из перечня (`Amenity`), валюта отображения цен (`UZS` / `USD` / `RUB`) и кнопка «Search».

Панель фильтров — это белая карточка с мягкой тенью и скруглёнными углами; весь интерфейс выдержан в едином стиле: градиентный фон сине-зелёного оттенка, тёмно-синий текст `#17324d`, карточки на белом.

---

### Отели и номера — рядом

Центральная часть страницы разделена на два блока:

**Отели** — таблица с колонками: миниатюра фото (88×56 px, с закруглёнными углами), ID, название, город, звёздность, тип размещения, описание. Фотографии в таблице подбираются динамически по названию города: Ташкент, Самарканд, Бухара получают свои тематические снимки; все остальные города — единый фолбэк. Клик по строке отеля мгновенно подгружает его номера в соседнюю таблицу.

**Номера** — таблица с колонками: ID, номер комнаты, тип, вместимость, цена за ночь (в выбранной валюте), план питания, список удобств. Цены показываются уже сконвертированными — курсы берутся из конфигурации приложения.

Обе таблицы — по 520 px в высоту, с плавным горизонтальным скроллом при большом числе колонок.

---

### Бронирование прямо из UI

После выбора номера активируется панель **Booking Actions**. Она проведёт вас через все три шага жизненного цикла бронирования — именно в той последовательности, которую реализует бэкенд:

1. **Create Booking** — заполняете данные гостя (имя, фамилия, PINFL из 14 цифр, email, телефон) и нажимаете кнопку. Номер уходит в статус `HOLD`.
2. **Confirm Booking** — подтверждаете бронирование. Статус меняется на `CONFIRMED`. Кнопка становится активной только после первого шага.
3. **Pay Booking** — выбираете валюту оплаты, вводите имя и токен плательщика. Деньги идут через Transaction Processing Service, статус — `PAID`.
4. **Cancel Booking** — доступна после создания. Отменяет бронирование с автоматическим расчётом возврата по политике.

Кнопки включаются и отключаются строго по состоянию: нельзя нажать «Confirm», не создав бронирование, нельзя оплатить до подтверждения. Текущий статус отображается в карточке прямо над формой.

Форма гостя предзаполнена демо-данными (`John Doe`, тестовый PINFL и email), чтобы можно было сразу попробовать весь флоу, не вводя ничего вручную.

---

### Отзывы об отелях

В нижней части страницы — панель **Hotel Reviews**. Она появляется сразу при выборе отеля: подгружаются все отзывы (до 50), показывается их количество и средний рейтинг. Тут же можно оставить новый отзыв: указать ID гостя, выставить оценку от 1 до 5 и написать текст. После отправки список обновляется автоматически.

Таблица отзывов содержит: ID, имя гостя, рейтинг, текст комментария, дату создания.

---

### Техническая организация

UI состоит из двух файлов:

- **`MainLayout.java`** — обёртка-шаблон (`AppLayout`): верхняя навигационная панель с логотипом «J-Booking» и кнопкой открытия бокового меню. Боковое меню содержит единственную ссылку — «Hotel Explorer».
- **`HotelExplorerView.java`** — единственное представление (`@Route(value = "", layout = MainLayout.class)`), реализует всю логику: фильтры, таблицы, бронирование, отзывы. Напрямую инжектирует сервисы (`CityService`, `HotelService`, `RoomService`, `BookingService`, `HotelReviewService`) — никаких REST-вызовов «по кругу», работа идёт через слой сервисов напрямую.

> Все изображения загружаются с Unsplash по публичным URL — для их отображения нужен доступ в интернет.
